# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (never commit secrets)
.env
.env.*
!.env.example

# TypeScript incremental build info
*.tsbuildinfo

# Claude Code per-machine settings
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright capture pipeline — assets generated locally, not source.
demo-assets/
playwright-report/
test-results/

# Final export package — only the binary assets are local; text artifacts
# (scripts, shot order, subtitles) are committed.
demo-export/assets/mobile/
demo-export/assets/desktop/
